{
  "name": "vitest-browser-commands-monorepo",
  "type": "module",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@11.20.0",
  "scripts": {
    "build": "pnpm -r run build",
    "lint": "eslint .",
    "fix": "eslint --fix . && prettier --write .",
    "test": "pnpm -r run -s test",
    "typecheck": "tsc -b",
    "prepare:playwright": "playwright install"
  },
  "devDependencies": {
    "@ocavue/eslint-config": "^4.13.0",
    "@ocavue/tsconfig": "^0.7.1",
    "@types/node": "^24.13.3",
    "eslint": "^10.8.0",
    "playwright": "^1.62.1",
    "prettier": "^3.9.6",
    "typescript": "^6.0.3"
  }
}
